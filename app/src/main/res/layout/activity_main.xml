<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/blue"
    android:padding="16dp">

    <!-- match_parent - размер объекта займет весь экран устройства или все оставшееся место в корневом ViewGroup -->
    <!-- wrap_content - размер объекта займет столько, сколько занимает его содержимое -->

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="start"
        android:paddingBottom="40dp"
        android:text="@string/app_name"
        android:textColor="@color/white"
        android:textSize="@dimen/main_textSize" />

    <Button
        android:id="@+id/btn_search"
        android:layout_width="match_parent"
        android:layout_height="@dimen/main_btn_layout_height"
        android:layout_marginBottom="16dp"
        android:layout_weight="@dimen/main_btn_layout_weight"
        android:backgroundTint="@color/white"
        android:text="@string/search"
        android:textColor="@color/black"
        android:textSize="@dimen/main_textSize"
        app:icon="@drawable/ic_search"
        app:iconGravity="textStart"
        app:iconPadding="@dimen/main_btn_iconPadding"
        app:iconTint="@color/black"
        app:cornerRadius="@dimen/main_btn_cornerRadius" />

    <Button
        android:id="@+id/btn_library"
        android:layout_width="match_parent"
        android:layout_height="@dimen/main_btn_layout_height"
        android:layout_marginBottom="16dp"
        android:layout_weight="@dimen/main_btn_layout_weight"
        android:backgroundTint="@color/white"
        android:text="@string/library"
        android:textColor="@color/black"
        android:textSize="@dimen/main_textSize"
        app:icon="@drawable/ic_library"
        app:iconGravity="textStart"
        app:iconPadding="@dimen/main_btn_iconPadding"
        app:iconTint="@color/black"
        app:cornerRadius="@dimen/main_btn_cornerRadius" />

    <Button
        android:id="@+id/btn_settings"
        android:layout_width="match_parent"
        android:layout_height="@dimen/main_btn_layout_height"
        android:layout_marginBottom="12dp"
        android:layout_weight="@dimen/main_btn_layout_weight"
        android:backgroundTint="@color/white"
        android:text="@string/settings"
        android:textSize="@dimen/main_textSize"
        android:textColor="@color/black"
        app:icon="@drawable/ic_settings"
        app:iconGravity="textStart"
        app:iconPadding="@dimen/main_btn_iconPadding"
        app:iconTint="@color/black"
        app:cornerRadius="@dimen/main_btn_cornerRadius" />
</LinearLayout>